import Typography from 'typography';

const typography = new Typography({
  baseFontSize: '16px',
  baseLineHeight: 1.3,
  scaleRatio: 2.75,
  headerFontFamily: ['Candela Bold'],
  headerWeight: 400,
  bodyFontFamily: ['Lato'],
  bodyWeight: 400
});

// Export helper functions
export default typography;
